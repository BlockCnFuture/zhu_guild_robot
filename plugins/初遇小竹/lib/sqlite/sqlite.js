import sqlite3 from 'sqlite3';
import path from 'path';
import { fileURLToPath } from 'url';

let dir = path.dirname(fileURLToPath(import.meta.url));
//const db = new sqlite3.Database(`${dir}/data.db`);

//export default db;